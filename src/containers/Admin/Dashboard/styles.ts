import { COLOR_CODE } from '@components';

const colorPalettes = [COLOR_CODE.PRIMARY_400, '#E697FF'];

const cardStyles = {
  borderRadius: 3,
  border: `1px solid ${COLOR_CODE.GREY_200}`,
  gap: 1.5,
  padding: '16px',
};

export { colorPalettes, cardStyles };
