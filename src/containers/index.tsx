import { Stack, Typography } from '@mui/material';

const Container = () => {
  return (
    <Stack>
      <Typography variant="h1">This site for staff</Typography>
    </Stack>
  );
};

export default Container;
